# Rose Pine Dawn theme colors
set -g @SESSION_HIGHLIGHT "#ea9d34"     # Hostname and session name color (gold)
set -g @WINDOW_ACTIVE_BG "#dfdad9"      # Background for active window (highlight medium)
set -g @WINDOW_ACTIVE_SEP "#b4367a"     # Separator color for active window (love)
set -g @WINDOW_INACTIVE_SEP "#907aa9"   # Separator color for inactive windows (iris)
set -g @TEXT_MAIN "#575279"             # Main text color (text)
